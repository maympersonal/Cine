@model IEnumerable<Backend.Models.Empleado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Gerente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContrasenaE)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Gerente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContrasenaE)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdE">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdE">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdE">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
